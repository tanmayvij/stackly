rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // TODO: replace with proper auth-scoped rules before production
      allow read, write: if true;
    }
  }
}